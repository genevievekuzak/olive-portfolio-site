---
import StickyNav from "./StickyNav.astro";
import '../style.css'
---

<html>
   <head>
          <meta charset="UTF-8" />
   </head>
   <section class="hero-section">
    <StickyNav />

      <div class="slider">
         <div class="slides">
            <div class="slide first">
               <img src="/assets/Intro-Pics/1.jpg" alt="woods intro image">
            </div>
            <div class="slide">
               <img src="/assets/Intro-Pics/2.jpg" alt="woods intro image">
            </div>
            <div class="slide">
               <img src="/assets/Intro-Pics/3.jpg" alt="woods intro image">
            </div>
            <div class="slide">
               <img src="/assets/Intro-Pics/4.jpg" alt="woods intro image">
            </div>
            <div class="slide">
               <img src="/assets/Intro-Pics/5.jpg" alt="woods intro image">
            </div>
         </div>
         <button class="prev">&#10094</button>
         <button class="next">&#10095</button>
      </div>

</section>
</html>

<script>

   const slides = document.querySelectorAll<HTMLElement>(".slides img");
   let slideIndex = 0;
   let intervalId = null;

   const nextButton = document?.querySelector(".next")?.addEventListener('click', () => {
      nextSlide();
   })

   const prevButton = document?.querySelector(".prev")?.addEventListener('click', () => {
      prevSlide();
   })

   initializeSlider();
   
   function initializeSlider() {
      slides[slideIndex].classList.add("displaySlide");
      // intervalId = setInterval(nextSlide, 5000);
   }

   function showSlide(index: number) {
      
      if (index >= slides.length) {
         slideIndex = 0;
      } else if(index < 0) {
         slideIndex = slides.length - 1;

      }
      
      slides.forEach(slide => {
         slide.classList.remove("displaySlide");
      });
      slides[slideIndex].classList.add("displaySlide");
   }

   function prevSlide() {
      slideIndex--;
      showSlide(slideIndex);
   }

   function nextSlide() {
      slideIndex++;
      showSlide(slideIndex);
   }
   
   
</script>


<style>

   .slider {
      position: relative;
      width: 80vw;
      margin: auto;
      overflow: hidden;
      margin-block-start: 6rem;
      display: flex;
      justify-content: center;
      align-items: center;
      
      .slides {
         height: 650px;
      }

      img {
         display: none;
         /* opacity: 0;
         transition: opacity 1s; */
      }

      img:first-child {
         opacity: 1;
      }
   }

   img.displaySlide {
      display: block;
      height: 40rem;
      /* animation-name: fadeIn;
      animation-duration: 1.5s; */

   }

   .slider button {
      position:absolute;
      top: 50%;
      transform: translateY(-50%);
      font-size: 2rem;
      background-color: white;
      color: black;
      border: none;
      cursor: pointer;
   }

   .prev {
      left: 0;
   }

   .next {
      right: 0;
   }

   @keyframes fadeIn {
      from {opacity: .5}
      to {opacity: 1}
   }
   
   @keyframes fadeOut {
      from {opacity: 1}
      to {opacity: 0}
   }

   @media screen and (max-width: 80em) {
      .slider {
         width: 90vw;

         .slides {
         height: 500px;
         }
      }

      img.displaySlide {
      display: block;
      max-height: 500px;
      object-fit: cover;
      }

      .slider button {
         font-size: 1.5rem;
      }
   }

   @media screen and (max-width: 500px) {
      .prev {
       background:rgba(255, 255, 255, 0.719)!important;
       backdrop-filter:blur(7px);-webkit-backdrop-filter:blur(7px);
       border-color:rgba(255, 255, 255, 0)!important;
      }

      .next {
      background:rgba(255, 255, 255, 0.719)!important;
       backdrop-filter:blur(7px);-webkit-backdrop-filter:blur(7px);
       border-color:rgba(255, 255, 255, 0)!important;
      }
   }

   @media screen and (min-width: 1500px) {
      .slider {
         width: 70vw;
      }
   }

   @media screen and (max-width: 650px) {
      .slider {
         .slides {
            height: 300px;
            .slide {
               height: inherit;
               display: inline-flex;
            }
         }
      }

      img.displaySlide {
       object-fit: contain;
       width: 90vw;
       height: auto;
       position: relative;
       top: auto;
      }


   }

</style>

